# Telegram Username Availability Checker

![Status](https://img.shields.io/badge/Status-Discontinued-red)
![Python](https://img.shields.io/badge/Python-3.x-blue)
![Platform](https://img.shields.io/badge/Platform-Heroku-purple)

> [!CAUTION]
> **ПРОЕКТ ЗАКРЫТ И БОЛЕЕ НЕ ПОДДЕРЖИВАЕТСЯ**
>
> Обслуживание данного репозитория полностью прекращено. Автор больше не использует данный код в продакшене, обновления выпускаться не будут. Использование текущей версии скрипта не принесет результата из-за критических изменений на стороне целевой платформы.

## 📄 О проекте

Данный репозиторий представляет собой архивную версию распределенной системы для проверки доступности коротких юзернеймов (3-5 символов) в мессенджере Telegram.

Архитектура решения строилась на связке **Master-Worker**, где:
*   **Worker (`worker.py`)**: Развертывался на множественных инстансах (Heroku Dynos), генерировал комбинации имен, проверял их через HTTP-запросы и парсинг HTML-страниц `t.me`.
*   **Master (`master.py`)**: Агрегировал результаты из бинарных файлов (`pickle`), синхронизируемых через Google Drive, и обновлял сводные таблицы в Google Sheets.

## 🛑 Причина остановки (Technical Post-Mortem)

Работоспособность скрипта была нарушена глобальными изменениями в экосистеме Telegram.

В связи с интеграцией платформы **Fragment** и введением аукционов для NFT-юзернеймов, команда Telegram внесла существенные изменения в DOM-структуру публичных веб-страниц (`t.me/<username>`).

Реализованный в данном боте метод парсинга, основанный на библиотеке `BeautifulSoup4` и поиске CSS-класса `tgme_action_button_new` (а также мета-тегов `twitter:app:url:googleplay`), полностью утратил актуальность. Текущая структура страниц требует принципиально иного подхода к анализу занятости адреса, либо использования официального API Fragment/Telegram Client API (MTProto), что выходит за рамки данной реализации.

## 🛠 Технический стек

*   **Язык:** Python
*   **Инфраструктура:** Heroku (управление через `heroku3`)
*   **Данные:** Google Drive API (хранение pickle-дампов), Google Sheets API (`gspread` для отчетности).
*   **Парсинг:** BeautifulSoup4, Requests.
*   **Параллелизм:** `concurrent.futures`, `_thread`.

## 📜 История изменений (Change Log)

Ниже приведена хронология разработки финальной версии системы:

| Дата | Изменение |
| :--- | :--- |
| **14.02.2021** | 💾 Полный переход на хранение массивов данных в бинарных файлах `pickle` (отказ от текстовых логов для оптимизации I/O). |
| **11.02.2021** | 📊 Завершена реализация модуля генерации выходных логов и отчетности в `master.py`. |
| **29.01.2021** | 🚀 Релиз первой стабильной рабочей версии `master`-скрипта. |
| **28.01.2021** | ✅ Финализация логики `worker`-скрипта (версия, представленная в репозитории). |
